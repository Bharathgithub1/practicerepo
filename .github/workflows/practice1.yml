name: first-workflow
on: 
  workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print hello 
      - run: echo "hello"
      - name: print key
      - run: echo "goodbye"
    
  
